server:
  port: 8888


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/angelestrada14019/proyecto-integrador-2
          default-label: dev-back-donaciones
          force-pull: true
          search-paths: config-data


