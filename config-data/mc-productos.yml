server:
  port: ${SERVER_PORT:0}
spring:
  rabbitmq:
    host: 54.90.87.91
    port: 5672
    username: guest
    password: guest
  application:
    name: mc-productos
  ## MySQL
  datasource:
    username: ${mysql_username}
    password: ${mysql_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://db.ctd.academy:3306/${mysql_database}


#cola
queue:
  pi2:
    gp2:
      prductos_donaciones: queue.pi2.gp2.productos_donaciones

#eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://${IP_EUREKA:localhost}:8761/eureka
  instance:
    hostName: 3.82.230.170
    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
#actuator
management:
#  health:
#    circuitbreakers:
#      enabled: true
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
info:
  name: mc-productos
  description: mc-productos
  version: 1.0.0
  date: 2023-11-02
